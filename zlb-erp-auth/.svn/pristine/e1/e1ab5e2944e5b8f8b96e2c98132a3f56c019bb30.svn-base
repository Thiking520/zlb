package com.zhilianbao.erp.auth.mapper.employee;

import java.util.List;

import com.zhilianbao.erp.auth.entity.employee.EmployeePointListBean;
import com.zhilianbao.erp.auth.vo.employee.EmployeePointListVo;
import com.zhilianbao.erp.auth.vo.employee.EmployeeVo;
import com.zhilianbao.erp.common.vo.ResponseResult;

import tk.mybatis.mapper.common.Mapper;

public interface EmployeePointListMapper extends Mapper<EmployeePointListBean> {

	/**
	 * 通过员工ID查找该员工所有配送站点数据
	 * 注：包括deleted == ture的
	 * @param eplVo
	 * @return
	 */
	List<EmployeePointListVo> findAll(EmployeePointListVo eplVo);

	/**
	 * 传入多个ID放入ids中
	 * 批量更新状态，所有都修改为vo中deleted字段的值
	 * @param epVo
	 */
	void batchModifyStatus(EmployeePointListVo epVo);

	/**
	 * 通过员工ID查找该员工所有配送点
	 * 注：只查有效数据
	 * @param vo
	 * @return
	 */
	ResponseResult<EmployeePointListVo> findAllPoints(EmployeeVo vo);
}