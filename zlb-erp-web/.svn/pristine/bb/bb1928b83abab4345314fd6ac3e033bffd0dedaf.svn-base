projectWebRoot = projectWebRoot || "http://192.168.1.99:8080/";

function initSysPath() {
	if ($('#a_mainPage')) {// 主界面
		$('#a_mainPage').attr("href", projectWebRoot);
	}
	if ($('#a_regAccountBtn')) {// 注册
		$('#a_regAccountBtn').attr("href", projectWebRoot + "smember/regmobile.html");
	}
	if ($('#a_goLogin')) {
		$('#a_goLogin').attr("href", projectWebRoot);
	}
	if ($("#a_forgetpassword")) {
		$("#a_forgetpassword").attr("href", projectWebRoot + "smember/forgotPassword.html");
	}
	// $.ajax({
	// type : 'get',
	// url : '../pub/pubController/initSysPath?r=' + new Date(),
	// dataType : 'json',
	// success : function(data) {
	// console.info(data);
	// }
	// });
}